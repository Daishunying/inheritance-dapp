import React from "react";
import ReactDOM from "react-dom/client";
import InheritanceDApp from "./InheritanceDApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InheritanceDApp />
  </React.StrictMode>
);
